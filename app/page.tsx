import React from 'react'
import Nav from './Nav'

type Props = {}

const page = (props: Props) => {
  return (
    <div className='overflow-hidden'>
      <Nav />
    </div>
  )
}

export default page
